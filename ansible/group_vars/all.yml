---
# Global configuration variables for dotfiles installation

# User configuration
user: "{{ ansible_user_id }}"
home: "{{ ansible_env.HOME }}"
dotfiles_dir: "{{ ansible_env.HOME }}/dotfiles"

# Optional installations (set to true to enable, or pass via --extra-vars)
install_cloudflare_warp: false
install_opencode: true
install_neovim: true
configure_git_defaults: true

# Tool versions
java_version: "jdk-24"
maven_version: "3.9.11"
go_version: "1.25.1"
kind_version: "v0.29.0"
node_version: "24"

# Git configuration (will be prompted during setup.sh or pass via --extra-vars)
git_name: ""
git_email: ""

# Monitor configuration (will be auto-detected if not set)
monitor1: ""  # Primary monitor
monitor2: ""  # Secondary monitor
